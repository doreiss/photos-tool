# Build-only dependency: produces dist/photos-tool.app via scripts/build-app.sh (PyInstaller).
# Deliberately NOT in pyproject [project.dependencies] so the runtime/wheel stays clean.
# (PyInstaller --windowed bundles osxphotos's full tree via --collect-all; py2app could not.)
pyinstaller>=6.0
